# Azure Data Factory exporter handoff

Artifact status: **starter/reference**

Project: `__PROJECT_NAME__`  
Scenario: `__SCENARIO__`

This is a credential-free placeholder for a later ADF exporter. ADF pipeline
JSON should be derived from `../pipeline/pipeline.json`; it must not become a
second source of workflow truth. Linked services, integration runtimes, account
IDs and secrets are intentionally absent from V1.

The canonical executable reference remains local Docker Compose with Spark on
demand and dbt-duckdb for Gold.

